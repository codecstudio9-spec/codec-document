
  # CODEC DOCUMENT

  This is a code bundle for CODEC DOCUMENT. The original project is available at https://www.figma.com/design/nIForcKZSHQNq5HBPf0v2o/CODEC-DOCUMENT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  