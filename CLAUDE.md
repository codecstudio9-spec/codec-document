# Codec Document — instructions for Claude

## Standing authorization (no need to ask each time)

The user has explicitly asked to skip confirmation prompts for the following routine actions in this repo. Do them directly, then report what was done when finished — don't pause mid-task to ask permission first:

- `git push origin master` (regular commits, not force-push)
- Supabase: `supabase db push`, `supabase functions deploy <name>`, `supabase link` — always with `--workdir` pointed at this project folder (see gotcha below) and `--yes` to skip interactive prompts
- `vercel deploy` / anything that ships this app to its existing Vercel project

Still stop and ask first for anything genuinely destructive or one-way: force-push, `git reset --hard`, dropping/truncating tables, deleting data, rotating/revoking credentials, or touching a Vercel/Supabase project other than this app's own.

## Known environment gotcha

The Supabase CLI on this machine does not reliably detect this project's `supabase/` folder as workdir (the path has spaces/parentheses: `CODEC DOCUMENT (2)\CODEC DOCUMENT`). Without `--workdir`, it silently latches onto a stray leftover `supabase/` folder in `C:\Users\hp` from an unrelated old project, and commands fail with confusing "entrypoint not found" errors. Always pass `--workdir "C:\Users\hp\Downloads\CODEC DOCUMENT (2)\CODEC DOCUMENT"` explicitly on every `supabase link` / `db push` / `functions deploy` call.

**Recurring gotcha (happened 2026-09-18 and again 2026-09-20)**: the Supabase CLI's login session periodically switches to authenticating against a DIFFERENT one of the user's Supabase accounts/projects ("estimateprobuild51-oss's Project", `xczrlsipuxtrkyjmmvzd`) instead of Codec Document (`yxzchnldmfsgdtbjurey`). Symptoms: `supabase projects list` shows the wrong project, or `supabase migration list`/`db push` fail with a 403 "does not have the necessary privileges" error. Fix: ask the user to run `supabase login --workdir "C:\Users\hp\Downloads\CODEC DOCUMENT (2)\CODEC DOCUMENT"` again (an interactive browser login only they can complete), then re-verify with `supabase projects list` before trusting any migration/deploy state. Don't assume a migration was never applied just because `migration list` looks empty/wrong — check this first.

**Edge functions with their own auth scheme (API keys, cron-triggered, etc.) must deploy with `--no-verify-jwt`.** Confirmed 2026-09-20: `api-v1` (the public REST API, authenticated via `cd_live_...` keys) was deployed without it, so the Supabase gateway rejected every external call as an invalid JWT before the function's own code ever ran — 100% unreachable from any outside integration despite correct application code. Same reasoning already applied to the public `dian-*` functions. When adding a new function that authenticates callers itself (not via a Supabase user session), deploy it with `--no-verify-jwt` from the start.

**A root-level `supabase_*.sql` file being present in this repo is NOT evidence it was ever actually applied.** Confirmed three separate times (`user_documents`/`profile_documents` missing columns, `finalize_document` never waiting for all signers, `generate_api_key` crashing on `gen_random_bytes`) — these files were meant to be run once by hand from the Supabase Dashboard SQL editor, and there's no reliable record of whether that happened. If you're touching anything these files describe, re-apply the relevant `CREATE OR REPLACE FUNCTION`/`ALTER TABLE ... ADD COLUMN IF NOT EXISTS` as a real tracked migration in `supabase/migrations/` instead of assuming it's already live. **Also**: `pgcrypto` functions (`gen_random_bytes`, `digest`) live in the `extensions` schema on this project, not `public` — any `SECURITY DEFINER` function with `SET search_path = public` can't see them and will fail at runtime (not at creation time) the first time a real authorized caller actually reaches that code path. Use native Postgres instead: `sha256(bytea)` instead of `digest(..., 'sha256')`, `gen_random_uuid()` (core since PG13) instead of `gen_random_bytes()`. An anonymous/unauthorized test of such a function proves nothing beyond "the function exists and correctly rejects unauthorized callers" — it never reaches the buggy line. Test as a real authorized user before trusting it works.

## Session log (for continuity across machines — this repo is the source of truth, not any one machine's local Claude memory)

**2026-09-19/20**: MFA for admins went from mandatory (accidentally locked the real admin out) to opt-in/off-by-default with an escape hatch on every blocking screen. "Crear nuevo" was rebuilt from an AI-based formatter into pure-code parsing (`utils/parse-pasted-document.ts`, `utils/highlight-key-info.ts`) — no AI call, faster, free — renamed to "Crea un documento nuevo," gained an optional per-signer name/ID/email form, dropped the redundant "Fecha" line, and got a real `html2canvas` → `html2canvas-pro` fix (Tailwind v4's `oklch()` colors crashed the old library) applied both there and in the main signed-document download path (`preview-page.tsx`). Biometric verification (WebAuthn Face ID/Touch ID) was added to the signing flow. Certicámara got scaffolding (admin pastes the key in Settings) but no real API integration yet (no credentials/docs from Certicámara). Multi-signer support turned out to already exist in the DB (`signers`/`signing_links` tables, per-signer tokens) from an earlier session — it just wasn't connected to "Crea un documento nuevo," and had a real bug (removing an "extra signer" orphaned their DB row, permanently blocking `finalize_document`). Both are fixed now. The public REST API (`api-v1`, `cd_live_...` API keys) and webhooks system existed but were completely non-functional — `api-v1` was deployed without `--no-verify-jwt` (Supabase's gateway rejected every external call before the function's own auth ran), webhooks had no dispatcher ever built, and `generate_api_key` crashed on a `pgcrypto` schema issue. All three fixed and live-tested end-to-end (generate key → call `GET /company` → real data back). New admin-only panel at `/dashboard/admin/api` for key/webhook management. See `git log` for the full commit-by-commit detail and this project's Claude memory files (Windows machine only) for the deeper narrative behind each fix.

## International SEO (US English + 6 LATAM countries Spanish)

Architecture fixed 2026-07-29 — see `middleware.ts`, `src/app/data/hreflang-clusters.ts`, `src/app/components/seo-head.tsx`, `src/app/components/structured-data.tsx`, `scripts/generate-sitemap.mjs`.

- **URL structure**: distinct URLs per language/market (English slugs for US state/city pages, Spanish slugs for the 6 LATAM country pages) — this is intentional and Google-recommended; never migrate to `/us/` or `/latam/` path prefixes, it would force a redirect migration of ~190 already-indexed URLs for no SEO benefit.
- **Canonical/hreflang**: authoritative source is the HTTP `Link` response header set by `middleware.ts` (Vercel Edge Middleware, runs on every non-excluded route) — this is what non-JS-executing crawlers see. `SEOHead.tsx` reinforces the same values client-side once React mounts. **Never** hardcode a canonical/hreflang in `index.html` again — it's a single static shell served for ~190 different routes, so anything hardcoded there is correct for at most one of them.
- **hreflang is for genuine translations only**: `hreflang-clusters.ts` only registers pages that are the SAME content translated (currently empty — no such pair exists yet). A California lease-agreement page and a Colombia lease-agreement page are NOT hreflang counterparts — different documents, different local law, different markets. Don't add a "this is the LatAm version of that US page" entry; that tells Google they're interchangeable, which is false and can hurt both pages.
- **Content language must be deterministic per URL, not IP-based**: the app-wide `LanguageProvider` (`language-context.tsx`) still auto-detects language via IP/browser locale for the interactive app — but every SEO landing page template (`CountrySignatureLanding`, `DocTypeStateLanding`, `CityDocTypeLanding`, `CityLegalDocumentsLanding`, `StateLegalDocumentsLanding`, `StateDocumentLanding`) is wrapped in `FixedLanguageProvider` with a hardcoded default (`es` for LATAM country pages, `en` for everything US) so the crawlable snapshot never changes based on Googlebot's crawl IP. When adding a new landing page/template in this family, wrap it the same way — don't let it read the raw IP-detected `useLanguage()` for its primary content.
- **`StructuredData`**: pass `language` + `country` (with the country's real `lawBadgeEn`/`lawBadgeEs`, e.g. from `latam-signature-seo-content.ts`) on any non-US page — never let the US-specific ESIGN Act / "United States" JSON-LD claims render unchanged on a LatAm country page, that's a false legal claim in structured data, not just a copy mismatch.
- **Sitemap**: `public/sitemap.xml` is generated by `npm run generate:sitemap` (auto-runs as part of `npm run build` — see the `postbuild`-style chain in package.json's `build` script). Don't hand-edit it; add a new page to `routes.tsx` and it's picked up automatically, unless it needs excluding (see `EXCLUDE_PREFIXES`/`EXCLUDE_EXACT` in the script — keep in sync with `middleware.ts`'s `EXCLUDED` regex).
- **Thin-content checklist for any new document×state/country landing page**: ~600+ words of genuinely unique text (not just the state/country name swapped into a template), at least one real local-law citation (see `signature-jurisdictions.ts` / `doctype-state-seo-content.ts` for the pattern), unique meta title+description, its own FAQ (not just the generic site-wide one from `StructuredData`), and a real internal link to/from a hub page for that document type so the cluster reads as deliberate, not spun. Same topic-cluster principle as `feedback_seo_article_standard.md` (blog articles), applied to landing pages.

## Stack

Vite + React 18 + TypeScript + Tailwind + Supabase (project ref `yxzchnldmfsgdtbjurey`) + react-router v7. Dev server port 5174. All Supabase signature/document operations live in `src/lib/signatureService.ts`; company/enterprise-module operations in `src/app/services/company-service.ts`.
